apiVersion: v1
kind: Service
metadata:
  name: indian-road-runner
  labels:
    app: indian-road-runner
spec:
  type: NodePort
  selector:
    app: indian-road-runner
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30070
      protocol: TCP
